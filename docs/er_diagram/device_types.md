```mermaid
erDiagram
    DEVICE_TYPES {
        text code PK "CI, SE, SR, SF, ST, SP, SO, GW"
        text description "Inverter, Power Meter, DI..."
        text category "Gateway | Device | Sensor"
    }
```